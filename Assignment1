#include <stdio.h>

// I'm thinking of creating the structure like a linked list. 
// I think that's what they want us to and I think it'll be easiest that way.
// We just make a node for each string and then print the entire linked list.
// Let me know what u think

struct Token {
	char data; 
	struct Token *next;
};

typedef struct Token Token;

Token *CreateToken( char * string ) {

		Token *newtoken; // creates a new empty node
       		newtoken  = (Token*)malloc(sizeof(Token));   
  		return NULL;
}

void DestroyToken( TokenizerT * tk ) {
   
    Token *prev = head;
    Token *cur = head;
    while(cur!=NULL) {
        prev = cur;
        cur = prev->next;
        free(prev);
    }
}



char *GetNextToken( TokenizerT * tk ) {

  return NULL;
}


int main(int argc, char **argv) {

  return 0;
}
